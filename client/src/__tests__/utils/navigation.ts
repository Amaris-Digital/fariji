export const mockNavigate = jest.fn()

export const useNavigate = () => mockNavigate

describe('navigate utils', () => {
    test('example test', () => {
      expect(1 + 1).toBe(2);
    });
  });
  
  
  
  